# `StateToolsProcess`

状态工具处理模块，位于输入处理之后、最终回复生成之前。

## 主要职责

- 根据上下文补充或修正状态信息
- 组织工具执行结果
- 为最终提示词构建提供额外状态数据

## 主要文件

- `StateToolsLMM.py`：状态工具处理主逻辑
- `config.py`：该模块相关配置
- `__init__.py`：模块导出

## 使用场景

`src/service.py` 会在构建最终提示词前调用该模块，以确保 `Context` 中包含完整的状态信息。
