# `InputProcess`

输入处理模块，负责在进入主模型之前整理用户输入与相关记忆。

## 主要职责

- 判断输入是否需要扩写
- 召回短期记忆与长期记忆
- 生成更适合下游模型使用的输入内容
- 将处理结果写入 `Context`

## 主要文件

- `InputLMM.py`：输入处理主逻辑
- `__init__.py`：模块导出

## 使用场景

该模块通常由 `src/service.py` 在请求处理流程中调用，位于状态工具处理之前、回复生成之前。
